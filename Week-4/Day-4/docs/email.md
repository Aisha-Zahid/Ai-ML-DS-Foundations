# Email automation (Task 2)

On book / reschedule / cancel, the assigned employee receives:

- Meeting time  
- Property  
- Client name + phone  
- Requirements / notes  

**Mock**: `outbox/email/*.json`  
**Live**: `EMAIL_MODE=live` + `RESEND_API_KEY` (+ verified `EMAIL_FROM`)
